# TRIAGE-DP Layer 4 — EM-HSD v2 (sentence-level constrained DP paraphrase)
#
# Packages: 123/ or em-hsd-v2/ (install editable after pip deps)
#
# Python 3.10+ | NVIDIA GPU >= 6 GB VRAM for Unsloth Qwen (GTX 1060 tested)
#
# Install (includes Layers 1–3 / SPINE deps):
#   pip install -r requirements-layer-4.txt \
#     --extra-index-url https://download.pytorch.org/whl/cu118
#   pip install -e 123/          # or: pip install -e em-hsd-v2/
#
# Runtime:
#   export PYTHONPATH="123/src:Johnny t0-1.03/src"   # adjust for em-hsd-v2/
#   export EM_HSD_ALLOW_DOWNLOADS=1
#   export TORCHDYNAMO_DISABLE=1   # GTX 10xx (sm_61) with Unsloth
#
# llama.cpp backend (em-hsd-v2/): also needs cmake, build-essential, curl;
#   bash em-hsd-v2/scripts/build_llama_cpp.sh
#   bash em-hsd-v2/scripts/start_llama_server.sh

-r requirements-layers-123.txt

# Qwen3.5 paraphrase (Unsloth GPU path)
unsloth==2026.4.6
datasets==4.3.0
trl==0.24.0
peft==0.18.1
bitsandbytes==0.49.1

# llama.cpp OpenAI-compatible client (em-hsd-v2 production config)
openai>=1.0
