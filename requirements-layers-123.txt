# TRIAGE-DP Layers 1–3 + SPINE substrate (Johnny t0-1.03)
#
# Layer 1: cross-saliency triage (H(t)/A(t) occlusion, DP-MLM routing)
# Layer 2: stylometric / Biber priors (rule-based; no extra ML packages)
# Layer 3: TO calibration (harness trade-off search on dev split)
#
# Python 3.10+ | Linux x86_64 recommended
#
# Install:
#   pip install -r requirements-layers-123.txt \
#     --extra-index-url https://download.pytorch.org/whl/cu118
#
# Runtime:
#   export PYTHONPATH="Johnny t0-1.03/src"
#   export EM_HSD_ALLOW_DOWNLOADS=1    # HF hate classifiers + MLM + embeddings
#   python Johnny\ t0-1.03/scripts/setup_lexicons.py
#
# Optional one-time assets:
#   python Johnny\ t0-1.03/scripts/setup_models.py

numpy>=2.0
scikit-learn>=1.3
pyyaml>=6.0
regex>=2023.0

# Hate probes, MLM (dpmlm), saliency occlusion, harness utility / re-ID
torch==2.6.0+cu118
torchvision==0.21.0+cu118
torchaudio==2.6.0+cu118
transformers==5.5.0
sentence-transformers>=3.0
accelerate>=1.12
safetensors>=0.4
huggingface_hub>=0.23

pytest>=8.0
